import styled from 'styled-components';

const CompleteLinesWrapper = styled.div`
  width: 30%;
  height: 100%;

  & > span {
    display: inline-block;
    font-weight: bold;
  }
`;

export default { CompleteLinesWrapper };